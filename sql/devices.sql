/*
SQLyog Ultimate
MySQL - 8.0.28 
*********************************************************************
*/
/*!40101 SET NAMES utf8 */;

create table `devices` (
	`id` bigint (50),
	`tower_name` varchar (150),
	`device_name` varchar (150),
	`ip` varchar (150),
	`ptp` varchar (150),
	`models` varchar (300),
	`os` varchar (150)
); 
