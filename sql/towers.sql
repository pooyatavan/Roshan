/*
SQLyog Ultimate
MySQL - 8.0.28 
*********************************************************************
*/
/*!40101 SET NAMES utf8 */;

create table `towers` (
	`id` bigint (50),
	`tower_name` text ,
	`top_pos` varchar (150),
	`left_pos` varchar (150),
	`address` varchar (150)
); 
